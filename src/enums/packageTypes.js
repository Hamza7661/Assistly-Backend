const PACKAGE_TYPES = {
  FREE_TRIAL: 'free-trial',
  BASIC: 'basic',
  PRO: 'pro',
  PREMIUM: 'premium',
  CUSTOM: 'custom'
};

const PACKAGE_BILLING_CYCLES = {
  MONTHLY: 'monthly',
  YEARLY: 'yearly',
  ONE_TIME: 'one-time'
};

const PACKAGE_CURRENCIES = {
  USD: 'USD',
  EUR: 'EUR',
  GBP: 'GBP'
};

module.exports = {
  PACKAGE_TYPES,
  PACKAGE_BILLING_CYCLES,
  PACKAGE_CURRENCIES
};
